#include "main.h"

/**
 * _abs - computes the absolute value of an int
 * @n: input that is to be described
 * Return: n, -n
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
