#include <stdio.h>
/**
 * main - base 16
 * Return: always (0)
 * description: using %x to convert base 16 to hexidecimal
 **/

int main(void)
{
	int num;
	
	for (num = 1; num < 17; num++)
	
	{	
	
	scanf(int num);
	
	putchar("%x");

	putchar(int num);
	
	}

	putchar('\n');

	return (0);
}
