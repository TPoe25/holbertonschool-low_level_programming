#include "main.h"
#include <stdio.h>

/**
 * print_numbers - prints numbers
 *
 * Return: 0
 */

void print_numbers(void)
{
	int num;

	for (num = 0; num < 10; num++)

	{
		printf("%d", num);
	}
	_putchar('\n');
}
